# Examples

- [`basic/`](basic) — a Material site using the bundled plugins, built with
  `--strict`, plus the two failures worth seeing on purpose: a broken link, and
  a "last updated" date of 1970.

Each example pins the image tag. An example that uses `latest` demonstrates
something different every month, which is the opposite of this image's point.
